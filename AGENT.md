# このリポジトリでの進め方

エージェント(Claude)がこのリポジトリで作業するときの進め方を記録する。
経緯と判断の理由は `docs/adr/` と `docs/experiments/` にあり、ここに書くのは**作業の運び方**だけ。

## 前提となる制約

作業環境はLinuxのコンテナで、**Swiftツールチェインもオーディオデバイスも無い。**
この2つの欠落が、以下のほぼすべての進め方を規定している。

- Swiftのコードを書いても、その場ではコンパイルすら確認できない
- 音声に関わる挙動は一切実行できない

## 検証の3層

確かめたいことを、どこで確かめられるかで3つに分ける。**上の層で潰せるものを下の層に持ち込まない。**

| 層 | 確認できること | 手段 |
|---|---|---|
| この環境 | 言語非依存のロジック、生成物の妥当性 | Pythonでの数値実験、生成したplistのパース、YAML/TOMLの構文 |
| CI (GitHub Actions, macOS) | Swiftのコンパイル、ユニットテスト、バンドル組み立て、署名 | `swift test`、`codesign`、`plutil` |
| Mac実機 | マイク入力、TCCの許可、実際の音 | 人が手で実行し、結果を共有する |

実機でしか分からないことは実機に回すが、**それ以外はできるだけ上の層へ寄せる。**
`MeerkatsCore` が AVFoundation に依存しないのはこのためで、音声キャプチャ以外を
CIで検証できるようにするのが目的。

### CIで実機の挙動を予測できた例

ad-hoc署名でのCDHashの変化(変更なし→同一、変更あり→別)をCIで比較したところ、
それがそのまま実機でのTCCの許可の維持・喪失に対応した。実機を触る前に当たりをつけられる場合がある。
詳細は [`docs/experiments/macos-app-bundle-spike-result.md`](docs/experiments/macos-app-bundle-spike-result.md)。

## 設計判断はADRに残す

判断は `docs/adr/` にADRとして記録する。ステータスは Accepted / Proposed / Superseded。

- **採用しなかった選択肢とその理由も書く。** 特に、一見よさそうで実は問題が違うもの
  (例: 論理クロックは因果順序の手法であって物理時刻の対応づけではない)は、
  明示的に除外しておかないと後で蒸し返される。
- **前提が未検証なら、そう書く。** 「〜の見込み」「実挙動は未検証」と明記し、
  何をすれば決着するかまで書く。
- 検証で判明したら**断定に書き換える。** 「可能性がある」で放置しない。

## 実験は experiments に残す

実験コードは `experiments/` に置き、`mise` のタスクから実行できるようにする。
結果の数値は、決定の根拠ならADR本文に、独立した検証なら `docs/experiments/` にレポートとして残す。

## PRの分割とレビュー

**1つのPRは100行程度、多くても300行程度に収める。**

これは好みの問題ではなく、次の2つのためにそうする。

- **レビューとその対応が回る。** 大きなPRは指摘が積み上がり、直すたびに全体を読み直すことになる。
- **後から誤りが見つかったときに、原因の箇所を特定しやすい。** 粒度が粗いと、
  どの判断がまずかったのかをコミットから追えない。

例えば「記録コアの純粋ロジック」はこれ1つでは大きすぎる。テストの導入、フレームの切り直し、
集約、発話判定、時計、というように3〜5個に割る。

そのうえで、

- 1つのPRは**CIがグリーンになる単位**にする。動かない状態を積み残さない。
- マージ前にサブエージェントによる自己レビューを行う。この環境でコンパイルも実行もできない以上、
  **論理の誤りは誰も捕まえない**ため、手計算した値やインデックス計算を重点的に見てもらう。
- レビューで問題が無ければマージしてよい。

### テストが古い挙動を固定していないか見る

不具合を直したとき、既存のテストがその不具合のほうを期待値にしていることがある。
**テストが落ちたら、まず「コードが間違っているのか、テストが古いのか」を判断する。**
実際に、発話判定のフロアが固定される不具合を直した際、その固定を確認していたテストが落ちた。

## 報告のしかた

- **予測が外れたら、外れたと書く。** 当たった場合も、何が根拠だったかを残す。
- **自分の誤りは自分で指摘する。** 検証手順の不備、取りこぼした条件、作り込んだバグ。
  特に、結果が出たあとに「実はその手順では確かめられていなかった」と気づいた場合は、
  黙って直さず、何が確かめられていなかったのかを明示する。
- 数値は丸めずに出す。「おおむね良好」ではなく「-26.06 dBFS」と書く。
